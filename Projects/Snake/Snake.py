import pygame
pygame.init() #initialize the game
dis=pygame.display.set_mode((400,300)) #created the display of the game.
pygame.display.update() #This code updates the display of the game. 
pygame.quit() #uninitialize the game
quit()